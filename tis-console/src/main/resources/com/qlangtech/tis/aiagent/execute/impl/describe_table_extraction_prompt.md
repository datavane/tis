以下给出通过解析用户提交的描述任务内容，结合系统提供的json结构说明，输出结构化json解析结果的例子：# 用户输入内容：用户提供的需要的表信息：${extraTableInfo}数据源中已有表：${existTables}说明：所有的表大小写忽略# 输出json结构说明：根据 `用户输入内容` 的内容，需要推断出用户需要的目标表集合。返回的是一个json结构文本内容，有两个属性：1. targetTables： 需要抽取的目标表列表，类型是Array列表2. lostTables：在`用户输入内容`中明确说了需要某个表，但是没有在`数据源中已有表`中匹配成功的表列表，类型是Array列表``` json{  "targetTables":["table1","table2"]  "lostTables":["table3","table4"]}```以下是几个例子，其他情况请您举一反三。例子A，用户输入：```text用户提供的需要的表信息：aa,bb,dd数据源中已有表：aa,bb,cc```返回：``` json{  "targetTables":["aa","bb"]  "lostTables":["dd""]}```例子B，用户输入：```text用户提供的需要的表信息：除AA以外的所有表数据源中已有表：aa,bb,cc```返回：``` json{  "targetTables":["bb","cc"]  "lostTables":[]}```例子C ，用户输入：```text用户提供的需要的表信息：以aa作为前缀的所有表数据源中已有表：aa1,aa2,bb,cc```返回：``` json{  "targetTables":["aa1","aa2"]  "lostTables":[]}```重要：请确保返回的是有效的JSON格式，不要包含markdown标记或其他文本。