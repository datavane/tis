<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body {
            font-family: Arial, "Microsoft YaHei", sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            background-color: #f44336;
            color: white;
            padding: 20px;
            border-radius: 5px 5px 0 0;
        }
        .content {
            background-color: #f9f9f9;
            padding: 20px;
            border: 1px solid #ddd;
            border-radius: 0 0 5px 5px;
        }
        .info-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 15px;
        }
        .info-table th {
            background-color: #e0e0e0;
            padding: 10px;
            text-align: left;
            width: 30%;
            border: 1px solid #ccc;
        }
        .info-table td {
            padding: 10px;
            border: 1px solid #ccc;
        }
        .status-failed {
            color: #f44336;
            font-weight: bold;
        }
        .status-lost {
            color: #ff9800;
            font-weight: bold;
        }
        .status-canceled {
            color: #9e9e9e;
            font-weight: bold;
        }
        .link-button {
            display: inline-block;
            padding: 10px 20px;
            background-color: #2196F3;
            color: white;
            text-decoration: none;
            border-radius: 3px;
            margin-top: 15px;
        }
        .footer {
            margin-top: 20px;
            padding-top: 20px;
            border-top: 1px solid #ddd;
            color: #666;
            font-size: 12px;
        }
    </style>
</head>
<body>
    <div class="header">
        <h2>⚠️ ${title}</h2>
    </div>

    <div class="content">
        <p><strong>${subject}</strong></p>

        <table class="info-table">
            <tr>
                <th>任务名称</th>
                <td>${jobName}</td>
            </tr>
            <tr>
                <th>任务状态</th>
                <td>
                    #if(${status} == "失败")
                        <span class="status-failed">${status}</span>
                    #elseif(${status} == "丢失")
                        <span class="status-lost">${status}</span>
                    #elseif(${status} == "已取消")
                        <span class="status-canceled">${status}</span>
                    #else
                        ${status}
                    #end
                </td>
            </tr>
            #if(${startTime})
            <tr>
                <th>开始时间</th>
                <td>${startTime}</td>
            </tr>
            #end
            #if(${endTime})
            <tr>
                <th>结束时间</th>
                <td>${endTime}</td>
            </tr>
            #end
            #if(${duration})
            <tr>
                <th>运行时长</th>
                <td>${duration}</td>
            </tr>
            #end
            #if(${restart})
            <tr>
                <th>重启信息</th>
                <td>第 ${restartIndex} 次重启 / 总计 ${totalRestart} 次</td>
            </tr>
            #end
        </table>

        #if(${link})
        <p>
            <a href="${link}" class="link-button">查看Flink Web UI</a>
        </p>
        #end

        <div class="footer">
            <p>此邮件由 TIS 平台自动发送，请勿直接回复。</p>
            <p>如有问题，请联系系统管理员。</p>
        </div>
    </div>
</body>
</html>
