# 需求说明生成一份 HttpRequestProxy 插件的简要使用说明文档（格式为markdown格式，注意不要生成乱码），HttpRequestProxy 类的路径为：/Users/mozhenghua/j2ee_solution/project/tis-solr/tis-plugin/src/main/java/com/qlangtech/tis/plugin/proxy/HttpRequestProxy.javaTIS的用户可以通过该文档可以快速了解该插件的核心作用，并且了解插件工作机制。# 文档素材1. 插件类，通过插件类中带有`@FormField`标注的字段，可以了解插件向用户暴露的属性，用户就在在前端页面中通过这些属性操作插件的。另外，插件类中的一些业务执行逻辑也可以作为素材。2. 插件的属性描述文件，插件描述文件存放在相同工程的resources目录下，例如：HttpRequestProxy类的类路径为java/com/qlangtech/tis/plugin/proxy/HttpRequestProxy.java，则在同工程的resources目录下会存在以下两个文件：   1. HttpRequestProxy.json：路径为 src/main/resources/com/qlangtech/tis/plugin/proxy/HttpRequestProxy.json，内部是插件中被`@FormField`标注的属性的markdown格式的字段元数据说明，例如：默认值，前端显示的placeholder、label信息、help使用说明   2. HttpRequestProxy.md：该文件可能没有，内部是 插件中被`@FormField`标注的属性的markdown格式的字段说明，没有字段都会以 `## fieldName` 这样起头3. HttpRequestProxy 插件类中的 带有`@TISExtension`标注的插件描述类（如：DefaultHttpRequestProxyDescriptor），内有 validate和verify作为前缀的方法，作为插件的校验约束规则可作为素材。     需要综合以上素材信息，生成有针对性的说明文档，避免泛泛而谈。# 内容要求内容清晰，不要涉及过多的技术细节（阅读者可能为非技术人员，在文档中不要出现任何代码脚本），文档字数控制在400字以内。# 生成文档路径生成的文档存放在相同工程的resources目录下，需要与插件类在同名的包路径下，文件名的生成规则为，在插件类名后加一个`_introduce`后缀。例如：HttpRequestProxy类的类路径为java/com/qlangtech/tis/plugin/proxy/HttpRequestProxy.java则生成文件为：src/main/resources/com/qlangtech/tis/plugin/proxy/HttpRequestProxy_introduce.md